{
  "name": "project-manager-spa",
  "version": "1.0.0",
  "description": "SPA para gestión de proyectos internos - RIWI",
  "scripts": {
    "api": "json-server --watch db.json --port 3000",
    "dev": "npx serve . --listen 5500",
    "start": "concurrently \"npm run api\" \"npm run dev\""
  },
  "devDependencies": {
    "json-server": "^0.17.4",
    "concurrently": "^8.2.0",
    "serve": "^14.2.0"
  }
}
